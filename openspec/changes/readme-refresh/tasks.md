# Tasks: README 文档刷新（可执行清单）

## 准备

- [ ] 读取当前 `README.md`、`package.json`、`index.html`，列出现有描述与事实差异

## 文档更新

- [ ] 更新 README 标题与一句话定位（保持现有风格即可）
- [ ] 更新/补齐 Features（只保留已实现项：增删改、优先级、localStorage 等）
- [ ] 修正 Tech Stack（严格以 `package.json` 为准）
- [ ] 补齐 Getting Started（npm install / npm run dev / 访问地址）
- [ ] 添加截图占位说明（不造成误导/404）
- [ ] 添加 Contributing 与 License（如仓库未提供 license，则写明未指定）

## 验收

- [ ] 对照 specs/acceptance.md 逐条核对
- [ ] 在本地执行 `npm install` 与 `npm run dev`（若已有依赖，可至少 `npm run dev`）并确认页面可用

